# z/crates/z-cli/src/

## Responsibility

The `z-cli` crate is the **CLI binary entry point** for the `z` project manager. Its function is to mediate between the user (terminal/TUI) and the Zellij multiplexer, Git worktree infrastructure, and forge (GitHub) APIs. It implements all CLI subcommands (`open`, `close`, `delete`, `prune`, `notify`, `list`, `switch`, `logs`, `logs-viewer`, `actions`, `autopilot`) and the interactive TUI loop. It owns the concrete adapters for trait abstractions defined in `z_core` — process-backed I/O, file-backed persistence, and SSH-encapsulated remote execution.

**Non-responsibilities**: domain logic, data model definitions, layout composition, workflow DSL interpretation, forge data parsing — these are delegated to `z_core` and `z_autopilot`.

---

## Design Patterns

### 1. Trait-Adapter Separation (Ports & Adapters)
All domain interfaces are defined as traits in `z_core::traits` (ports). The CLI crate provides concrete, process-backed implementations (adapters):
- `ZellijSessionManager` → `SessionManager` — shells out to `zellij`
- `WtWorktreeManager` → `WorktreeManager` — shells out to `git worktree` / `wt`
- `GhForgeClient` → `ForgeClient` — shells out to `gh`
- `KdlProjectStore` → `ProjectStore` / `ProjectStoreWriter` — reads/writes `~/.config/z/projects.kdl`
- `FileActivityStore` → `ActivityStore` — reads/writes `~/.config/z/session-activity.json`
- `LocalWorktreeMetadataStore` → `WorktreeMetadataStore` — reads/writes `~/.config/z/worktree-metadata.json`
- `FileLogger` → `Logger` — appends to `~/.local/state/z/z.log`
- `ProcessDepChecker` → `DepChecker` — probes tool `--version` output
- `DispatchNotifier` → `Notifier` — fans out to file/macOS/Telegram channels
- `CliStepExecutor` → `StepExecutor` — runs autopilot commands locally or via SSH
- `FileRunStore` → `RunStore` — persists autopilot workflow run state
- `ZellijSessionRefresher` → `SessionRefresher` — async background TUI refresh
- `CliPreviewDataSource` → `PreviewDataSource` — TUI preview acquisition

### 2. Callback Inversion (TUI Integration)
The TUI loop (`z_tui::run_tui`) receives a `TuiCallbacks` struct of function pointers (`prune_fn`, `log_fn`, `swap_fn`, `kill_session_fn`, `add_project_fn`, `edit_project_fn`, `delete_project_fn`, `reload_fn`). This inverts control: the TUI invokes CLI operations without depending on CLI crate internals, keeping `z_tui` pure.

### 3. Process-as-Subsystem
Nearly all infrastructure interactions go through subprocess execution:
- Zellij management: `zellij [list-sessions | attach | delete-session | --session -n <layout>]`
- Worktree management: `git worktree list --porcelain`, `wt switch -c <branch>`, `wt remove <branch>`
- Forge queries: `gh pr view --json ...`, `gh run list --branch ...`
- Remote execution: `ssh -o ConnectTimeout=10 <host> bash -l -c '<cmd>'` or `mosh <host> -- bash -l -c '<cmd>'`
- Notifications: `osascript -e 'display notification ...'`, `curl -X POST https://api.telegram.org/bot<token>/sendMessage`

### 4. Single-Roundtrip Session Discovery
`ZellijSessionRefresher::fetch_all_sessions` and `build_entries` in `main.rs` make **one** `zellij list-sessions` call per refresh cycle, then filter by project prefix. Remote sessions are fetched in parallel via `std::thread::scope` with one SSH call per remote host.

### 5. Temporary File Protocol for Layouts
Zellij layouts are KDL documents generated by `z_core::layout::generate_layout_kdl`, written to `/tmp/z-layout-{PID}-{SEQ}.kdl` via `write_temp_layout`, then consumed by `zellij --session <name> -n <path>`. The counter uses `AtomicU64` for thread-safe unique paths.

### 6. KDL as Configuration Metalanguage
Project registration lives in `~/.config/z/projects.kdl` (parsed by `z_core::config::parse_projects_kdl`). Per-repo config lives in `<project>/.config/z.kdl`, parsed by `parse_repo_config_projection` which uses two-phase parsing: first raw KDL document, then projections via `parse_per_repo_config_doc` (for `PerRepoConfig`) and `parse_autopilot_workflows_doc` (for `Vec<AutopilotWorkflow>`). Global config lives in `~/.config/z/config.kdl` (parsed by `z_core::config::parse_global_config_kdl`). `repo_config::RepoConfigProjection` bundles the relevant slices for CLI consumers.

### 7. Shell Quoting for Remote Safety
All SSH command construction uses `remote::shell_quote` which wraps values in single quotes with embedded-quote escaping (`'\''` idiom), preventing shell injection through branch names, paths, or session names.

### 8. Composition of Notification Channels
`DispatchNotifier` is a composite notifier that conditionally includes `MacosNotifier` and `TelegramNotifier` based on `GlobalConfig::notifications`. Metadata-backed TUI badges are written by `cmd_notify` via the worktree metadata store, not by `DispatchNotifier`. Failures are best-effort: all notifiers are attempted and only the last error is returned.

### 9. Open Session Planning
`session_open::plan_open_session` implements a deterministic decision: given a project, branch, and live sessions, it constructs the target `Session` name (via `Session::new` which sanitizes `/` to `-`) and checks for an existing live match. The caller (`cmd_open`) branches on `existing_session.is_some()` to attach vs. create.

### 10. Pruning via Symmetric Normalization
`prune::find_orphaned_sessions` and `prune::find_orphaned_worktrees` use `sanitize_branch_name` on both the session branch and worktree branch before comparison, ensuring `/`-vs-`-` normalization doesn't produce false orphans.

---

## Data & Control Flow

### CLI Entry (`main` → `run`)
1. `main()` runs `ProcessDepChecker::check_deps()`. If any required tool is missing, prints errors and exits.
2. Calls `run()`, which matches on `args[0]` and dispatches:
   - `None` → `cmd_tui()` (interactive loop)
   - `"list"` → `cmd_list()`
   - `"open"` → `cmd_open(project, branch, prompt)`
   - `"close"` → `cmd_close(session)`
   - `"delete"` → `cmd_delete(session)`
   - `"prune"` → `cmd_prune(dry_run)`
   - `"notify"` → `cmd_notify(session, message, level)`
   - `"autopilot"` → `cmd_autopilot_dispatch(sub, args)`
   - `"logs"` → `cmd_logs(n)`
   - `"switch"` → `cmd_switch()`
   - `"logs-viewer"` → `cmd_logs_viewer()`
   - `"actions"` → `cmd_actions()`

### TUI Loop (`cmd_tui`)
1. Creates `KdlProjectStore`, loads `GlobalConfig`, loads built-in workflows once.
2. `loop`:
   a. Calls `build_entries` → fetches projects, one-shot `zellij list-sessions`, worktree counts, per-repo configs. Builds `Vec<ProjectEntry>`.
   b. Loads local and remote notification aliases from worktree metadata.
   c. Calls `z_tui::run_tui(...)` with a `TuiCallbacks` closure bundle.
   d. Matches `TuiAction` result:
      - `Open` / `New` / `NewFromIssue` / `NewFromPr` → `cmd_open` with optional prompt injection
      - `EditPerRepoConfig` → `cmd_edit_per_repo_config` (spawns `$EDITOR`)
      - `RunAction` → `zellij_action::run_action` (launches command in new tab/pane)
      - `RunWorkflow` → `cmd_autopilot_run`
      - `SwitchToSession` → selected dashboard-modal session, then no-op/switch/attach depending on current Zellij context
      - `Quit` → return

### Open Session Flow (`cmd_open`)
1. Resolve project from `KdlProjectStore`.
2. If remote (has `host`): delegate to `cmd_open_remote` → SSH `z open <project> <branch>` on host via `ssh -t` or `mosh`.
3. If local: call `ZellijSessionManager::list_sessions` → `plan_open_session`:
   - If existing live session → `attach_session` (zellij attach).
   - Else:
     a. If branch specified → resolve worktree via `WtWorktreeManager::list_worktrees` → reuse or `create_worktree` (which runs `git fetch origin` then `wt switch -c <branch>`).
     b. Merge global + per-repo config → `effective_layout`. Inject prompt if present. Inject Claude stop hook.
     c. `session_mgr.create_session(..., layout, theme)` → generates KDL layout, writes temp file, runs `zellij --session <name> -n <layout_path>`.

### Preview Data Flow (`CliPreviewDataSource`)
1. `load_git_preview`: resolves worktree path (local or remote), fetches git status/log/divergence via `build_git_preview_command` (a compound `sh -c` pipeline with `---Z-GIT-PREVIEW-SEP---` delimiters), parses via `parse_git_preview_output`.
2. `load_extra_preview`: spawns three threads in a scope — one each for `get_pr`, `get_ci_status`, `get_review_status` on the forge client. Also fetches Zellij session info via `zellij list-sessions --json`.

### Pruning Flow (`cmd_prune` / `prune_summary`)
1. Iterates all projects. For each: `list_sessions` + `list_worktrees`.
2. `find_orphaned_sessions`: sessions whose branch has no matching worktree branch (after `sanitize_branch_name` on both sides).
3. `find_orphaned_worktrees`: worktrees (excluding `main`/`master`) whose branch has no matching session.
4. `prune_summary` (TUI mode): kills sessions and removes worktrees immediately, returns status string.
5. `cmd_prune` (interactive): shows lists, prompts for confirmation, then acts.

### Switch Flow (`cmd_switch` / dashboard `SwitchToSession`)
1. `z switch` guard: must run inside a Zellij session (`ZELLIJ_SESSION_NAME` set). Dashboard switcher loads entries for an in-dashboard modal, accepts no current session, and attaches interactively from outside Zellij after the TUI exits.
2. Global lock file (`/tmp/z-switch.lock`) prevents concurrent invocations (cleanup via `LockGuard` Drop impl).
3. List all z-managed sessions with ages and notification counts → sort by recent activity → standalone `run_switch_picker` for `z switch`, or `Modal::SwitchPicker` for the dashboard.
4. On selection: no-op if current, `zellij action switch-session <name>` inside Zellij, or `zellij attach <name>` outside Zellij.

### Actions Flow (`cmd_actions`)
1. Guard: must run inside a Zellij session. Parses `project:branch` from `ZELLIJ_SESSION_NAME`.
2. Loads global + per-repo actions, merges with built-in actions via `z_core::action::merge_actions`.
3. Resolves actions against `ActionEnv` (project context + forge preview).
4. Picker via `run_action_picker` → executes selected action (`Run` as Zellij cmd, or `OpenUrl` via OSC 8 escape sequence).

### Autopilot Flow
1. `cmd_autopilot_run`: resolves project, loads workflows (built-in + per-repo), creates `CliStepExecutor` + `FileRunStore` + `DispatchNotifier` → `execute_workflow_run`.
2. `CliStepExecutor::run_command`: dispatches locally or via SSH depending on `self.host`. `run_remote_command` wraps cmd in `cd <cwd> && <cmd>` with shell quoting.
3. `FileRunStore`: delegates to `z_autopilot::persist::{load_run, save_run, delete_run}`.
4. Status/prune subcommands use `list_runs` / `prune_terminal_runs`.

### Notification Flow
1. `cmd_notify` → resolves notification target via `resolve_notify_target` (discovers worktrees from git), writes metadata-backed notification, then fans out to external channels via `DispatchNotifier::from_config`.
2. Metadata-backed notifications are stored in `~/.config/z/worktree-metadata.json` via `LocalWorktreeMetadataStore::add_notification`.
3. TUI reads notifications via `load_notification_aliases` or `ZellijSessionRefresher::fetch_notifications`, both of which query `notification_session_aliases` on the metadata store.
4. External channels (macOS `osascript`, Telegram `curl`) are dispatched conditionally based on `GlobalConfig::notifications`.

---

## Integration Points

### Upstream Dependencies

| Dependency | Role | Integration Mechanism |
|---|---|---|
| `z_core` | Domain models, traits, config parsing, layout gen, session entry mgmt, theme, template resolution, action resolution | Rust trait implementations + function calls through public API |
| `z_autopilot` | Workflow DSL, run loop, step execution, persistence | `execute_workflow_run`, `list_runs`, `prune_terminal_runs`; trait `StepExecutor`, `RunStore` implemented here |
| `z_tui` | Interactive terminal UI (TUI, switch picker, action picker, log viewer) | `run_tui(entries, ..., callbacks, preview_source, refresher, ...)`, `run_switch_picker`, `run_action_picker`, `run_log_viewer` |
| `kdl` | KDL document model for config parsing | Raw `KdlDocument` parse in `repo_config`, then domain parsers from `z_core` |
| `serde_json` | Activity store serialization | `FileActivityStore` reads/writes `SessionActivity` as JSON |

### External Process Contracts

| Process | Interface | Called From |
|---|---|---|
| `zellij` | CLI: `list-sessions`, `attach <session>`, `delete-session <session> --force`, `--session <name> -n <layout.kdl>`, `action new-tab`, `run --floating/-c [...]`, `action switch-session <name>` | `session_manager`, `zellij_action`, `switch` |
| `git` | `worktree list --porcelain`, `fetch origin`, `rev-list --left-right --count HEAD...@{u}`, `log --oneline -5`, `symbolic-ref --short HEAD`, `status --short` | `worktree_manager`, `git_preview` |
| `wt` (worktrunk) | `switch -c <branch>`, `remove <branch> [--force]` | `worktree_manager` |
| `gh` | `pr view <branch> --json ...`, `run list --branch <branch> --json ...` | `forge` |
| `ssh` | `-o ConnectTimeout=10 <host> bash -l -c '<cmd>'` | `remote`, `autopilot_runner`, `git_preview`, `worktree_manager`, `session_manager` |
| `mosh` | `<host> -- bash -l -c '<cmd>'` | `main::cmd_open_remote` |
| `osascript` | `-e 'display notification ... with title ...'` | `notify::MacosNotifier` |
| `curl` | `-s -X POST <url> --data <body>` | `notify::TelegramNotifier` |
| `$EDITOR` | Spawned as subprocess to edit per-repo config | `main::cmd_edit_per_repo_config` |

### File System Contracts

| Path | Format | Owned By |
|---|---|---|
| `~/.config/z/config.kdl` | KDL (global config: theme, nav, notifications, actions, review_tool) | `z_core::config::parse_global_config_kdl` |
| `~/.config/z/projects.kdl` | KDL (project list: name, path, host, transport) | `KdlProjectStore` / `z_core::config::parse_projects_kdl` |
| `<project>/.config/z.kdl` | KDL (per-repo: layout, custom workflows, autopilot settings, actions) | `repo_config::parse_repo_config_projection` |
| `~/.config/z/session-activity.json` | JSON map `{session_name: unix_epoch_secs}` | `FileActivityStore` |
| `~/.config/z/worktree-metadata.json` | JSON (worktree records, notifications, agent activity) | `LocalWorktreeMetadataStore` |
| `~/.local/state/z/z.log` | Structured text log (`[LEVEL] timestamp message`) | `FileLogger` |
| `~/.local/share/z/autopilot/` | JSON (workflow run state per project+name) | `FileRunStore` → `z_autopilot::persist` |
| `/tmp/z-layout-{PID}-{SEQ}.kdl` | KDL (temporary Zellij layout) | `session_manager::write_temp_layout` |
| `<cwd>/.claude/settings.json` | JSON (Claude stop hook injection) | `main::inject_claude_stop_hook` |
| `/tmp/z-switch.lock` | Empty lock file (PID content) | `main::cmd_switch` |

### Trait Implementations (Port-Adapter Map)

| Trait (defined in `z_core`) | Adapter Struct | Module |
|---|---|---|
| `SessionManager` | `ZellijSessionManager` | `session_manager` |
| `SessionRefresher` | `ZellijSessionRefresher` | `session_manager` |
| `WorktreeManager` | `WtWorktreeManager` | `worktree_manager` |
| `ForgeClient` | `GhForgeClient` | `forge` |
| `ProjectStore` | `KdlProjectStore` | `config_store` |
| `ProjectStoreWriter` | `KdlProjectStore` | `config_store` |
| `ActivityStore` | `FileActivityStore` | `activity_store` |
| `WorktreeMetadataStore` | `LocalWorktreeMetadataStore`, `RemoteWorktreeMetadataStore` | `worktree_metadata_store` |
| `Notifier` | `MacosNotifier`, `TelegramNotifier`, `DispatchNotifier` | `notify` |
| `DepChecker` | `ProcessDepChecker` | `depcheck_impl` |
| `Logger` | `FileLogger` | `log` |
| `RunStore` | `FileRunStore` | `autopilot_runner` |
| `StepExecutor` | `CliStepExecutor` | `autopilot_runner` |
| `PreviewDataSource` | `CliPreviewDataSource` | `preview` |

### Module Dependency Graph (within `z-cli`)

```
main ──────────────────────────────────────────────────────────────────┐
 ├── config_store ──► z_core::config                                    │
 ├── session_manager ──► z_core::traits::SessionManager                  │
 │    └── activity_store ──► z_core::activity                            │
 ├── session_open ──► z_core::domain                                     │
 ├── worktree_manager ──► z_core::traits::WorktreeManager                │
 │    └── remote                                                         │
 ├── workspace                                                           │
 │    └── repo_config ──► z_core::config                                 │
 ├── preview                                                             │
 │    ├── git_preview ──► remote                                         │
 │    ├── worktree_manager                                               │
 │    └── forge                                                          │
 ├── prune ──► z_core::domain                                            │
 ├── remote ──► session_manager (parse_zellij_sessions)                  │
 ├── notify                                                              │
 ├── zellij_action                                                       │
 ├── worktree_metadata_store ──► z_core::traits::WorktreeMetadataStore   │
 ├── log ──► z_core::log                                                 │
 ├── depcheck_impl ──► z_core::depcheck                                  │
 └── autopilot_runner ──► z_autopilot::lifecycle, z_autopilot::persist  │
      └── remote                                                         │
```
